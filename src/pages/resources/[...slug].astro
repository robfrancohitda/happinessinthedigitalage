---
import ArticleEntryPage from '../../components/editorial/ArticleEntryPage.astro';

import {
  getArticleStaticPaths,
  type ArticleEntry,
} from '../../lib/articles';

export function getStaticPaths() {
  return getArticleStaticPaths('resource');
}

interface Props {
  article: ArticleEntry;
}

const { article } = Astro.props;
---

<ArticleEntryPage article={article} />
