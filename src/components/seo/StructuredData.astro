---
interface Props {
  data: Record<string, unknown>;
}

const {
  data,
} = Astro.props;

const json =
  JSON.stringify(data)
    .replaceAll('<', '\u003c');
---

<script
  type="application/ld+json"
  is:inline
  set:html={json}
></script>
